async function main() {
  // We get the contract to deploy

  console.log(deposit, depsoit.address);
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });